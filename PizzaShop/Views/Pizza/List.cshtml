@model IEnumerable<Pie>

<div>
    <h1>List of Pies</h1>

    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th> Description </th>
                <th>Price</th>
                <th>Is Available</th>
                <th>Pie of the week</th>
                <th>category</th>
                
            </tr>
        </thead>
        <tbody>
            @foreach (var pie in Model)
            {
                <tr>
                    <td>@pie.GetName()</td>
                    <td>@pie.GetDescription()</td>
                    <td>@pie.GetPrice() </td>
                    <td>@(pie.GetInStock()    )</td>
                    <td>@(pie.GetIsPieOfTheWeek()) </td>
                    <td>@(pie.Category.GetCategoryName()) </td>
                </tr>
            }
        </tbody>
    </table>
</div>